//criar array
let listaFilmes = ['Yesterday', 'A Chegada', 'Escola de Rock']

//adicionar itens no array
listaFilmes.push('Harry Potter', 'Transformers')

for (let i = 0; i < listaFilmes.length; i++) {
    document.write(`<p>${listaFilmes[i]}</p>`)
}


